# build artifacts
# main.js is committed so users can install from GitHub without building.
*.js.map
styles.css.map

# dependencies
node_modules/

# vitest / coverage
coverage/

# editor / os
.DS_Store
*.log

# local plugin state / dev
data.json
.hotreload

# Hugo build output
docs/public/
docs/resources/
docs/.hugo_build.lock

# Outreach backlog (notes de promo, ne pas publier)
.claude/outreach/

# Demo/onboarding tooling
# Scenario fixtures (demo/NNN_*/demo-vault/) ARE committed; the vaults a take
# actually runs in live outside the repo, in ~/fileclass-demos/.
demo/node_modules/
