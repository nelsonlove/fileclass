baseURL = "https://mdelobelle.github.io/fileclass/"
languageCode = "en-us"
title = "Fileclass"
enableGitInfo = true

# Self-contained skeleton: minimal layouts live in ./layouts, so `hugo` builds
# offline with no external theme. Swap in a theme later if desired.

[menu]
  [[menu.main]]
    name = "Positioning"
    pageRef = "/positioning"
    weight = 10
  [[menu.main]]
    name = "Videos"
    pageRef = "/videos"
    weight = 15
  [[menu.main]]
    name = "Schema layer"
    pageRef = "/schema"
    weight = 20
  [[menu.main]]
    name = "Fields & input"
    pageRef = "/fields"
    weight = 30
  [[menu.main]]
    name = "UI surfaces"
    pageRef = "/ui"
    weight = 40
  [[menu.main]]
    name = "Views"
    pageRef = "/views"
    weight = 50
  [[menu.main]]
    name = "CLI & API"
    pageRef = "/cli"
    weight = 55
  [[menu.main]]
    name = "Settings"
    pageRef = "/settings"
    weight = 60
  [[menu.main]]
    name = "Modeling"
    pageRef = "/modeling"
    weight = 70

[markup.goldmark.renderer]
  unsafe = true

# The on-page outline in the right column. h2 and h3 only: the pages that need it
# are long because they have many sections, not because they nest deeply, and an
# outline you have to scroll is no longer a position indicator.
[markup.tableOfContents]
  startLevel = 2
  endLevel = 3
  ordered = false

[markup.highlight]
  # github-dark: light tokens on a dark background, matching the always-dark
  # code blocks in the theme (readable in both light and dark page modes).
  noClasses = true
  style = "github-dark"
  tabWidth = 2
